[package]
name = "module"
version = "0.1.0"
authors = ["unknowntrojan"]
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
winapi = { version = "*", features = ["psapi", "libloaderapi"] }